# SoulGuide 開發工作流程

## 📋 概述

SoulGuide 採用分佈式協同開發模式：
- **Manus 沙箱**：功能開發、測試、自動推送到 GitHub
- **GitHub Codespace**：代碼審查、DEBUG、測試驗證
- **GitHub**：代碼中心、版本控制、協同管理

## 🔄 自動化同步流程

### 工作流程圖

```
┌─────────────────────────────────────────────────────────┐
│              Manus 沙箱 (AI 開發)                       │
│  • 開發功能                                             │
│  • 編寫測試                                             │
│  • 驗證功能                                             │
└────────────────────┬────────────────────────────────────┘
                     │ auto-sync.sh (自動執行)
                     │ • git add -A
                     │ • git commit
                     │ • git push origin main
                     ↓
┌─────────────────────────────────────────────────────────┐
│           GitHub (代碼中心)                             │
│  • 版本控制                                             │
│  • 完整歷史記錄                                         │
│  • Pull Request 管理                                    │
└────────────────────┬────────────────────────────────────┘
                     │ git pull origin main
                     │ (手動或自動)
                     ↓
┌─────────────────────────────────────────────────────────┐
│         GitHub Codespace (您的工作)                     │
│  • DEBUG 代碼                                           │
│  • 測試功能                                             │
│  • 進行代碼審查                                         │
│  • 提交改進 (git push)                                  │
└─────────────────────────────────────────────────────────┘
```

## 🚀 開發步驟

### 1. 我在 Manus 中開發功能

```bash
# 功能開發
# • 編寫代碼
# • 運行測試
# • 驗證功能在開發服務器中正常工作
```

### 2. 自動同步到 GitHub

```bash
# auto-sync.sh 自動執行：
./scripts/auto-sync.sh

# 或手動執行：
git add -A
git commit -m "feat: Add new feature"
git push origin main
```

### 3. 您在 Codespace 中拉取更新

```bash
# 在 Codespace 中
git pull origin main

# 安裝依賴（如有新增）
pnpm install

# 重新啟動開發服務器
pnpm dev
```

### 4. 進行 DEBUG 和測試

```bash
# 在 Codespace 中測試功能
# • 檢查功能是否正常工作
# • 進行 DEBUG
# • 驗證代碼質量
```

### 5. 提交改進

```bash
# 如果您發現問題或進行了改進
git add .
git commit -m "fix: Fix issue in feature"
git push origin main

# 我會自動拉取您的更改
```

## 📁 項目結構

```
soulguide/
├── client/                 # 前端代碼
│   ├── src/
│   │   ├── pages/         # 頁面組件
│   │   ├── components/    # 可復用組件
│   │   ├── lib/           # 工具函數
│   │   └── _core/         # 核心邏輯
│   └── package.json
├── server/                # 後端代碼
│   ├── routers.ts         # TRPC 路由
│   ├── db.ts              # 數據庫操作
│   └── _core/             # 核心邏輯
├── drizzle/               # 數據庫 schema
├── scripts/
│   ├── auto-sync.sh       # 自動同步腳本
│   └── seed-categories.mjs # 數據初始化
├── package.json
└── DEVELOPMENT.md         # 本文件
```

## 🔧 常用命令

### 開發命令

```bash
# 安裝依賴
pnpm install

# 啟動開發服務器
pnpm dev

# 運行測試
pnpm test

# 構建生產版本
pnpm build

# 類型檢查
pnpm tsc
```

### Git 命令

```bash
# 查看狀態
git status

# 查看提交歷史
git log --oneline -10

# 創建新分支
git checkout -b feature/your-feature

# 切換分支
git checkout main

# 合併分支
git merge feature/your-feature

# 查看遠程分支
git branch -a
```

### 自動同步

```bash
# 執行自動同步腳本
./scripts/auto-sync.sh

# 或在 Manus 中自動執行（計劃任務）
# 每次功能開發完成後自動運行
```

## 📝 提交信息規範

提交信息應遵循以下格式：

```
<type>: <subject>

<body>

<footer>
```

### 類型（type）

- `feat`: 新功能
- `fix`: Bug 修復
- `docs`: 文檔更新
- `style`: 代碼風格調整
- `refactor`: 代碼重構
- `test`: 測試相關
- `chore`: 構建、依賴等
- `auto`: 自動同步

### 示例

```
feat: Add teacher verification system

- Implement file upload for certificates
- Add admin review interface
- Create verification status tracking

Closes #123
```

## 🔐 認證設置

### GitHub Token

自動同步使用 GitHub Personal Access Token 進行身份驗證：

```bash
# Token 已配置，無需手動設置
# Token 存儲在系統安全位置
```

## 📊 工作流程檢查清單

- [ ] 功能開發完成
- [ ] 代碼通過類型檢查（`pnpm tsc`）
- [ ] 測試通過（`pnpm test`）
- [ ] 代碼風格符合規範
- [ ] 提交信息清晰準確
- [ ] 自動同步到 GitHub
- [ ] Codespace 中拉取並驗證
- [ ] 功能測試通過
- [ ] 準備下一個功能

## 🐛 故障排除

### 自動同步失敗

```bash
# 檢查 Git 狀態
git status

# 檢查遠程配置
git remote -v

# 手動推送
git push origin main
```

### Codespace 無法拉取

```bash
# 檢查網絡連接
ping github.com

# 重新配置遠程
git remote set-url origin https://github.com/Alejo-7288/soulguide.git

# 手動拉取
git pull origin main
```

### 代碼衝突

```bash
# 查看衝突
git status

# 解決衝突後
git add .
git commit -m "resolve: Merge conflicts"
git push origin main
```

## 📞 支持

如有任何問題或建議，請：

1. 在 GitHub Issues 中報告
2. 提交 Pull Request 進行改進
3. 聯繫開發團隊

---

**最後更新**：2026-01-13
**版本**：1.0
